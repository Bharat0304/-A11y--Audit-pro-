{
  "name": "accessibility-analyzer",
  "version": "1.0.0",
  "description": "A11y Audit Pro - Comprehensive web accessibility analyzer for WCAG compliance",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev:client\" \"npm run dev:extension\"",
    "dev:client": "cd client && npm run dev",
    "dev:extension": "cd extension && npm run build:watch",
    "build": "npm run build:client && npm run build:extension",
    "build:client": "cd client && npm run build",
    "build:extension": "cd extension && npm run build",
    "install:all": "npm install && cd client && npm install && cd ../extension && npm install"
  },
  "keywords": [
    "accessibility",
    "wcag",
    "analyzer",
    "browser-extension"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "@tailwindcss/postcss": "^4.1.11"
  }
}